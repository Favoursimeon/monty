#include "monty.h"
/**
 * first_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void first_stack(top_stack **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
